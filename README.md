# Microframeworks-WEB
This project upgrades an existing web server into a full web framework. It will support backend REST services, allow developers to define endpoints with lambda functions, handle query parameters, and manage static files like HTML, CSS, JavaScript, and images.
